import { SLOView } from '@/components/SLO/SLOView';

export default function SLOPage() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', height: '100%' }}>
      <SLOView />
    </div>
  );
}
